dependencies:
  pre:
    - rm -rf ./node_modules
  cache_directories:
    - ~/.npm
  override:
    - nvm install 6 && npm install && grunt release
general:
  artifacts:
    - "public" # relative to the build directory