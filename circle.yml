dependencies:
  pre:
    - rm -rf ./node_modules
  cache_directories:
    - ~/.npm
  override:
    - nvm install 6 && npm install

#general:
#  artifacts:
#    - "public" # relative to the build directory

compile:
  override:
    - grunt release
  post:
    - mv public $CIRCLE_ARTIFACTS/
